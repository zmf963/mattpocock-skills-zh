---
name: to-spec
description: 将当前对话转化为一份 spec 并发布到项目的 issue 跟踪器——无需访谈，只需综合你已经讨论过的内容。
disable-model-invocation: true
---

这个 skill 获取当前对话上下文和对代码库的理解，产出一份 spec（你可能称这份文档为 PRD）。**不要**访谈用户——只需综合你已经知道的内容。

issue 跟踪器和 triage 标签词汇表应该已经提供给你——如果没有，运行 `/setup-matt-pocock-skills`。

## 流程

1. 探索仓库以了解代码库的当前状态，如果你还没有做的话。在整份 spec 中使用项目的领域词汇表术语，并尊重你触及区域中的任何 ADR。

2. 草拟你将在哪些接缝（seam）处测试该功能。优先使用已有接缝而非新建。尽可能使用最高层的接缝。如果需要新接缝，在你能达到的最高点提出。代码库中的接缝越少越好——理想数量是 1。

与用户确认这些接缝是否符合他们的预期。

3. 使用以下模板编写 spec，然后将其发布到项目的 issue 跟踪器。应用 `ready-for-agent` triage 标签——无需额外 triage。

<spec-template>

## 问题陈述

用户面临的问题，从用户的视角出发。

## 解决方案

问题的解决方案，从用户的视角出发。

## 用户故事

一长串编号的用户故事。每个用户故事应采用以下格式：

1. 作为<角色>，我想要<功能>，以便<收益>

<user-story-example>
1. 作为手机银行客户，我想要查看账户余额，以便对我的消费做出更明智的决策
</user-story-example>

这个用户故事列表应非常详尽，覆盖该功能的所有方面。

## 实施决策

一份已做出的实施决策列表。可以包括：

- 将要构建/修改的模块
- 这些模块中将被修改的接口
- 来自开发者的技术澄清
- 架构决策
- Schema 变更
- API 契约
- 具体交互

不要包含具体的文件路径或代码片段。它们可能很快变得过时。

例外：如果原型产出了一个比文字更能精确编码决策的片段（状态机、reducer、schema、类型结构），在相关决策中将其内联并简要注明它来自原型。裁剪到决策丰富的部分——不是可运行的 demo，只是重要部分。

## 测试决策

一份已做出的测试决策列表。包括：

- 什么构成一个好测试的描述（只测试外部行为，不测试实现细节）
- 哪些模块将被测试
- 测试的先例（即代码库中类似类型的测试）

## 范围外

对该 spec 范围外事项的描述。

## 补充说明

关于该功能的任何补充说明。

</spec-template>
